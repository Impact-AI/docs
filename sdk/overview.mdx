---
title: SDK Overview
description: "Impact SDKs for OpenTelemetry-native LLM observability"
icon: "code"
---

Impact provides official SDKs for Python and JavaScript/TypeScript.

| Language | Package | Install |
|---|---|---|
| Python | `impact` | `pip install impact` |
| JavaScript/TypeScript | `impact-ai` | `npm i impact-ai` |

Both SDKs share the same design principles and API semantics.

## Python SDK

Package name: `impact`

## What It Does

- Sends traces and logs to Impact over OTLP HTTP
- Auto-instruments supported AI providers/frameworks
- Attaches `impact.context.*` attributes across execution context
- Provides a single manual span decorator: `@trace`
- Works in BYO OpenTelemetry setups (`auto`, `attach`, `bootstrap`)

## Minimal Integration

```python
import impact
from openai import OpenAI

impact.init()
impact.context(user_id="u_123", interaction_id="int_456")

client = OpenAI()
client.chat.completions.create(
    model="gpt-4o-mini",
    messages=[{"role": "user", "content": "Hello"}],
)
```

## Public API

```python
import impact

impact.init(...)
impact.context(...)
impact.trace(...)
impact.instrument_asgi_app(app)
impact.shutdown()
```

## Runtime Modes

| Mode | Behavior |
|---|---|
| `auto` (default) | Attach to existing tracer provider when available, otherwise bootstrap |
| `attach` | Require existing provider and add Impact processors/exporters |
| `bootstrap` | Create and register a new provider |

## Endpoint Resolution

By default, `impact.init()` derives endpoint routing from `IMPACT_API_KEY` (`impact_<region>_*` format).

For self-hosted/custom deployments, override endpoint with:

- `api_endpoint` in `impact.init(...)`, or
- `IMPACT_BASE_URL` environment variable.

## Key Python-Specific Points

- `IMPACT_API_KEY` is required (argument or env var)
- SDK sets GenAI semantic-convention defaults for compatible instrumentations
- Context is stored via `contextvars`, so it works with async/threaded flows
- `shutdown()` flushes and stops Impact-owned processors/exporters

## JavaScript/TypeScript SDK

Package name: `impact-ai`

### What It Does

- Sends traces and logs to Impact over OTLP HTTP
- Auto-instruments supported AI providers/frameworks
- Attaches `impact.context.*` attributes across execution context
- Provides manual span tracing via `impact.trace()`
- Works in BYO OpenTelemetry setups (`auto`, `attach`, `bootstrap`)

### Minimal Integration

```typescript
import impact from "impact-ai";
import OpenAI from "openai";

impact.init();
impact.context({
  userId: "u_123",
  interactionId: "int_456",
});

const client = new OpenAI();
await client.chat.completions.create({
  model: "gpt-4o-mini",
  messages: [{ role: "user", content: "Hello" }],
});
```

### Public API

```typescript
import impact from "impact-ai";

impact.init({ ... });
impact.context({ ... });
impact.trace("name", fn);
impact.flush();
impact.shutdown();
impact.vercelAITelemetry();
```

## Recommended Next Pages

<CardGroup cols={2}>
  <Card title="Python Installation" icon="python" href="/sdk/installation">
    Install Python SDK and configure credentials.
  </Card>
  <Card title="JavaScript SDK" icon="js" href="/sdk/javascript">
    Full JS/TS SDK documentation.
  </Card>
  <Card title="Context" icon="link" href="/sdk/context">
    Attach user/session metadata to traces.
  </Card>
  <Card title="Supported Providers" icon="plug" href="/sdk/providers">
    Current instrumentation coverage.
  </Card>
</CardGroup>
